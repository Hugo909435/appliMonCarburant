name: mon_carburant_app
description: "Comparateur de prix des carburants en France : carte des
  stations, prix officiels du flux open data, favoris et coût réel du plein."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.5.0

  # Navigation
  go_router: ^14.0.0

  # Networking + open data parsing (donnees.roulez-eco.fr : zip -> xml)
  http: ^1.2.0
  archive: ^3.4.0
  xml: ^6.3.0

  # Persistence / cache
  path_provider: ^2.1.0
  shared_preferences: ^2.2.0

  # Geolocation ("stations près de moi")
  geolocator: ^12.0.0

  # Carte (OpenStreetMap, pas de clé API requise)
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  flutter_map_marker_cluster: ^1.3.0

  # Itinéraire vers une station (ouvre l'app de plans native)
  url_launcher: ^6.2.0

  # Graphique de tendance des prix
  fl_chart: ^0.68.0

  # Typographie
  google_fonts: ^6.2.1
  firebase_core: ^4.15.0
  firebase_auth: ^6.7.0
  cloud_firestore: ^6.10.0
  google_sign_in: ^7.2.0
  google_sign_in_web: ^1.1.3

  # Remontée des plantages en production (Android/iOS uniquement).
  firebase_crashlytics: ^5.0.0

  # « Se connecter avec Apple » : exigé par la règle 4.8 de l'App Store dès
  # lors qu'on propose une connexion tierce (ici Google).
  sign_in_with_apple: ^6.1.3
  # Hachage du nonce transmis à Apple (cf. AuthService.signInWithApple).
  crypto: ^3.0.6

  # Numéro de version affiché dans l'écran Compte, pour le support.
  package_info_plus: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Déclinaison des icônes et de l'écran de démarrage à partir des sources
  # produites par tool/generate_branding.dart (voir la section de config en
  # bas de ce fichier).
  flutter_launcher_icons: ^0.14.1
  flutter_native_splash: ^2.4.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  assets:
    - assets/data/departments.json
    - assets/data/sample_stations.json
    # Enseigne de chaque station (générée par tool/build_station_brands.dart)
    - assets/data/station_brands.json
    # Logos officiels optionnels : <clé>.png (voir lib/core/brands/brand_catalog.dart)
    - assets/logos/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# --- Icônes d'application -----------------------------------------------
# Régénérer après tout changement de assets/branding/ :
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  image_path: assets/branding/app_icon.png
  ios: true
  # L'App Store refuse toute transparence sur l'icône 1024 : l'aplatir ici
  # évite un rejet automatique à l'envoi.
  remove_alpha_ios: true
  android: ic_launcher
  adaptive_icon_background: "#111111"
  adaptive_icon_foreground: assets/branding/app_icon_foreground.png
  # Icône monochrome des thèmes dynamiques d'Android 13+.
  adaptive_icon_monochrome: assets/branding/app_icon_foreground.png
  web:
    generate: true
    image_path: assets/branding/app_icon.png
    background_color: "#111111"
    theme_color: "#111111"
  windows:
    generate: true
    image_path: assets/branding/app_icon.png

# --- Écran de démarrage --------------------------------------------------
# Régénérer après tout changement de assets/branding/ :
#   dart run flutter_native_splash:create
flutter_native_splash:
  color: "#FFFFFF"
  image: assets/branding/splash_logo_light.png
  color_dark: "#000000"
  image_dark: assets/branding/splash_logo_dark.png
  # Android 12+ impose son propre gabarit : le logo est posé dans un disque,
  # d'où des images dédiées, plus petites dans un cadre plus grand.
  android_12:
    color: "#FFFFFF"
    image: assets/branding/splash_logo_light_android12.png
    color_dark: "#000000"
    image_dark: assets/branding/splash_logo_dark_android12.png
  web: false
